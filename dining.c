// Morgan Mischo
// lab 5
// CS 3100

#include<stdlib.h>
#include "dininglib.h"


int main () {
    // pthread_t threads[count]; 
    // int indexes[count]; 
    // for(i = 0; i < count; i++) {
    //     indexes[i] = i; 
    //     rc = pthread_create(&threads[i], &attr, &threadFunction, &indexes[i];); 
    // }
}